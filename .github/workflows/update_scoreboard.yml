      - name: Commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          git config user.name "kumaresan-07"
          git config user.email "kumaresan-07@users.noreply.github.com"
          git add CONTRIBUTOR_SCOREBOARD.md
          git commit -m "Update contributor scoreboard" || echo "No changes to commit"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/kumaresan-07/kumaresan-07.github.io.git main
